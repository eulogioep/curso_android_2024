package com.eulogioep.introduccionakotlin

fun main(){

    val a = 1
    val b = 2
    var max = a
/*
    // If simple
    if (a < b) max = b

    // If con else y asignado directamente en la variable.
    max = if (a > b) {
        a
    }else{
        b
    }

    // if más simplicado.

    max = if (a > b) a else b
*/
    println(max)
}